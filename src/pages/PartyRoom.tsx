import React from "react";
import Button from "../reusable/Button";

const PartyRoom: React.FC = () => {
	return (
		<div>
			<Button word='+ Add friend' />
		</div>
	);
};

export default PartyRoom;
